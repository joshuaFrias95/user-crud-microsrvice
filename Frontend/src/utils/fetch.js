export class Fetch {
   /* BASE_URL = "http://localhost:";
    PORT;

    constructor(port) {
        this.PORT = port;
    }

    async get(path) {
        const URL = ${this.BASE_URL}${this.PORT}/api/v1${path};
        const res = await fetch(URL, {
        method: "GET",
        headers: {
          'Content-Type': 'application/json',
        },
       });

       const data = await res.json();
       return data;
    }*/
}

